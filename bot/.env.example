# /etc/forgejo-runner-notify.env — non-secret env for forgejo-runner-notify.service
# The actual chat ID stays on-host at /etc/forgejo-runner-notify.env (gitignored).
# Telegram + Codeberg tokens are sealed via systemd-creds; see bot/README.md.
TELEGRAM_CHAT_ID=<numeric-chat-id>
